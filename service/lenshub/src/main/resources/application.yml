server:
  port: 8080
  servlet:
    context-path: /api
  tomcat:
    max-http-form-post-size: 10MB
    max-swallow-size: 10MB
    max-parameter-count: 5000

vnpay:
  pay-url: ${PAY_URL}
  tmn-code: ${TMN_CODE}
  hash-secret: ${SECRET_KEY}
  return-url: ${RETURN_URL}
  version: ${VERSION:2.1.0}
  command: ${COMMAND:pay}
  order-type: ${ORDER_TYPE:other}

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
      enabled: true
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
  #    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
  #      password: your_password_if_any

  config:
    import: optional:file:.env[.properties]

  jpa:
    hibernate:
      ddl-auto: update # tự tạo / cập nhật bảng khi entity thay đổi (dev mode)
    show-sql: true # log câu SQL ra console
    properties:
      hibernate:
        format_sql: true # định dạng SQL cho dễ đọc
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false # khuyến nghị tắt để tránh lazy-loading lỗi

  # Nếu bạn dùng file .sql hoặc data.sql seed data
  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: debug # log câu SQL
    org.hibernate.type.descriptor.sql.BasicBinder: trace # log giá trị parameter

app:
  jwt:
    secret: ${APP_JWT_SECRET}
    expiration-ms: ${APP_JWT_EXPIRATION_MS:86400000}
    issuer: ${APP_JWT_ISSUER:zyna-app}
  activation:
    base-url: ${APP_ACTIVATION_BASE_URL:http://localhost:8080/api/auth/activate}
    ttl-hours: ${APP_ACTIVATION_TTL_HOURS:24}
    jwt-secret: ${APP_ACTIVATION_JWT_SECRET:change-me-activation-secret-32bytes-long}
  mail:
    from: ${APP_MAIL_FROM:no-reply@zyna.dev}
  password-reset:
    ttl-minutes: ${APP_PASSWORD_RESET_TTL_MINUTES:10}
  scheduler:
    birthday:
      cron: ${APP_SCHEDULER_BIRTHDAY_CRON:0 0 6 * * ?}
      enabled: ${APP_SCHEDULER_BIRTHDAY_ENABLED:true}
    voucher-expiring:
      cron: ${APP_SCHEDULER_VOUCHER_EXPIRING_CRON:0 0 7 * * ?}
      enabled: ${APP_SCHEDULER_VOUCHER_EXPIRING_ENABLED:true}
      days-before: ${APP_SCHEDULER_VOUCHER_EXPIRING_DAYS_BEFORE:3}
  inventory:
    low-stock:
      threshold: ${APP_INVENTORY_LOW_STOCK_THRESHOLD:5}
