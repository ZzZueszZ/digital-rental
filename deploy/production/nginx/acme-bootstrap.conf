server {
    listen 80;
    listen [::]:80;
    server_name api.lenshub.shop storage.lenshub.shop;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
    }

    location / {
        return 404;
    }
}
